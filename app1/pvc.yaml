apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sharedfs
  namespace: default
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: sharedfs
